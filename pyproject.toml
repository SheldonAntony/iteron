[project]
name = "iteron"
version = "0.1.0"
description = "First cost-accountable self-improving research agent"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[project.scripts]
iteron = "iteron.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
